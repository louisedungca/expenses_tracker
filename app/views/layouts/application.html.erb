<!DOCTYPE html>
<html>
  <head>
    <title>ExpensesTracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-stone-200">
    <main class="relative bg-stone-200 w-full h-screen flex flex-col items-center">
       <div class="w-4/5 sm:w-1/5 h-auto absolute top-[3.5rem] right-[0.5rem] z-30 ">
        <% flash.each do |type, message| %>
          <% if type == "notice" %>
            <div class="text-green-500 mt-2 py-2 px-4 rounded-lg text-xs tracking-wider font-semibold border-2 border-green-500 border-dashed bg-green-200">
              ** <%= message %>
            </div>            
          <% else %>
            <div class="text-red-500 mt-2 py-2 px-4 rounded-lg text-xs tracking-wider font-semibold border-2 border-red-500 border-dashed bg-red-200">
              ** <%= message %>
            </div>
          <% end %>          
        <% end %>
      </div>

      <%= yield %>     
    </main>
  </body>
</html>